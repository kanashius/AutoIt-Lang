# Language UDF for AutoIt

This is a UDF (User defined functions) library for AutoIt, providing a simple way to support different language and allow dynamically switching between them.

The example shows different ways to use the UDF.
The only required file to be included in the script is the "Lang.au3".